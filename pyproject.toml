[build-system]
requires = [
    "setuptools",
    "wheel",
    "numpy>=2.2.6",
    "cython>=3.3.0"
]

[tool.pylint.messages_control]
extension-pkg-whitelist = ["pydantic", "mrsimulator"]
disable = "R0201"
